<div class="show_container">
  <div class="show_core_container">
    <div class="form-group">
      <div class="show_container_target text-center show-eg">
        <p class="d-inline">Evento:</p>
       <h4>
         <%= @event.name  %>
       </h4> 
     </div>
     <div class="show_container_target tag_index text-center title-ins">
      <h5>Listado de inscriptos al grupo</h5>
     </div>
   </div>
 </div>

<div class="show_container">
  <div class="show_core_container_reg">
      <div class="form-group text-center">
          <ul class="list-group">
            <% @event.users.each do |user| %>
            <li class="list-group-item">
              <p><%= user.email %></p>
              <div class="flex-row"> 
                <%= button_to "Enviar confirmación", { action: "inscription_email", email: user.email, name: @event.name }, method: :post %>
                <%= button_to "Enviar recordatorio", { action: "reminder_email", email: user.email, name: @event.name }, method: :post %>
                <%= button_to "Enviar recordatorio", { action: "novedades_email", email: user.email, name: @event.name }, method: :post %>
              </div>
            </li>
            <% end %>
          </ul>
      </div>
  </div>
</div>

<div class="button_align_right">
  <%= link_to 'Atras', admin_events_path, class: 'button-show btn'%>
</div>